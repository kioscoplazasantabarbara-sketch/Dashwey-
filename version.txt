Dashwey v9.5.72 — Haptic Feedback System

SISTEMA CENTRALIZADO: triggerHaptic(type)
  Vive en Utils → disponible globalmente como Utils.triggerHaptic()

PATRONES:
  light  →  8ms   (tap, toggle, nav snap, drag start)
  medium → 15ms   (venta completada, drop card, guardar)
  error  → [10,30,10] (acción inválida, validación)

CAPACITOR-READY:
  Para upgrade a Capacitor Haptics, solo cambiar
  el bloque navigator.vibrate en triggerHaptic() —
  ningún call site cambia.

PUNTOS DE INTEGRACIÓN:
  · Bottom nav buttons (3)      → triggerHaptic('light')
  · goTab() — snap entre módulos → triggerHaptic('light')
  · startDrag()                 → triggerHaptic('light')
  · endDrag() — drop exitoso    → triggerHaptic('medium')
  · procesarPago() — cobro      → triggerHaptic('medium') ×2
  · Validación formularios      → triggerHaptic('error')
  · ALM confirmar pedido        → triggerHaptic('medium')
  · ALM tap artículo            → triggerHaptic('light')
  · Settings toggles            → triggerHaptic('medium')

NORMALIZADOS:
  Todos los Utils.haptic() y navigator.vibrate() directos
  reemplazados por triggerHaptic() — fuente única

NO AFECTADOS:
  vibrate:[100,50,100] en payload de notificaciones push (correcto)

RESTRICCIONES RESPETADAS:
  ✅ No en scroll
  ✅ No en animaciones automáticas
  ✅ No en gráficos
  ✅ Un solo haptic por acción de usuario
  ✅ Guard 'vibrate' in navigator antes de llamar

node --check: 10/10 ✅  |  16.378 líneas
