Dashwey v9.5.74

[AUDITORÍA FORENSE — 8 FIXES DE PRODUCCIÓN]

[1] BUG CRÍTICO — emitSync: variable 'ev' indefinida (ReferenceError silenciado)
    · DashweyBus.emitSync() usaba 'ev' en el catch — variable del scope de emit(), no de emitSync()
    · Resultado: el warn() lanzaba ReferenceError silenciado por try/catch
    · Todos los paths que usaban emitSync() fallaban silenciosamente
    · Fix: 'ev' → 'event' (parámetro local correcto)

[2] BUG — CURRENT_CACHE desactualizado en bootstrap HTML
    · Head bootstrap limpiaba caches antiguas pero CURRENT_CACHE = "dashwey-v9-5-51"
    · La cache activa "dashwey-v9-5-73" no estaba protegida → podía ser eliminada
    · Fix: actualizado a "dashwey-v9-5-74" (versión activa)

[3] BUG — sw.js postMessage versión incorrecta
    · SW enviaba SW_UPDATED con version: '9.5.32' (versión de hace 41 releases)
    · Fix: actualizado a '9.5.74'

[4] DUPLICATE — Listener vacío en #nv-save (modal nueva visita)
    · document.getElementById('nv-save', modal)?.addEventListener?.('click', () => {})
    · Registraba handler vacío ANTES del handler real → dos listeners en el mismo elemento
    · Además: getElementById() no acepta segundo argumento — siempre devolvía null
    · Fix: línea eliminada

[5] DUPLICATE — _applyDashMode llamada 2 veces en refresh()
    · render() tenía setTimeout(_applyDashMode, 380)
    · refresh() llama render() + setTimeout(_applyDashMode, 340)
    · Resultado: dos DOM passes casi simultáneos en cada refresh()
    · Fix: eliminado el setTimeout de render(), _applyDashMode() al final de render()
      (cache guard modeKey evita trabajo redundante si nada cambió)

[6] DUPLICATE — Dos click listeners en modal _openVisitasPeriodPicker
    · Listener 1: cerrar si click en fondo / Listener 2: mostrar form custom
    · Ambos corrían en cada click → posible interferencia entre handlers
    · Fix: fusionados en un único listener con early return

[7] PERF — _shimmerAllCards generaba 8 macrotask timers
    · _CARD_IDS.forEach → setTimeout(fn, i*40) → 8 entradas en el task scheduler
    · Fix: un único requestAnimationFrame + loop interno
      CSS animation-delay via --shimmer-delay custom property (stagger sin timers)

[8] PERF — setTimeout anidado en refresh() (340ms outer → 600ms inner)
    · Fix: aplanado — requestAnimationFrame para clase + setTimeout(600ms) directo
      _applyDashMode() eliminado del bloque (ya lo llama render())

[SW]       CACHE_NAME: dashwey-v9-5-73 → dashwey-v9-5-74
[manifest] start_url + iconos: v=9.5.51 → v=9.5.74
[HTML]     CURRENT_CACHE, title, meta, links: todos → 9.5.74

Líneas: 16.424  |  Fixes: 8/8 ✅
