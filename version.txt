Dashwey v9.5.73

[1] BARRAS DE GRÁFICA — pipeline correcto por periodo
    · Antes: usaba hoyVentas (filtro de hoy) → siempre vacío si periodo = mes/semana
    · Ahora: buckets adaptados por period.type:
      today/hours → 24 buckets horarios (todos los datos del periodo)
      week        → 7 buckets Lun-Dom
      month       → N buckets diarios (días del mes)
      year        → 12 buckets mensuales
      custom      → 30 días rolling
    · Resultado: siempre hay barras cuando existen ventas

[2] POPUP DE BARRA — total + transacciones
    · Tap/hover en barra muestra: "HH:00: €XX.XX · N ventas"
    · Calcula txnBuckets paralelo al buckets de €

[3] CSS DUPLICADO eliminado
    · .drag-handle-hint estaba definido 2 veces con posicionamientos contradictorios
    · Eliminada la primera definición (centered old), conservada la nueva (mejorada)
    · Handle centrado correctamente: left:50% transform:translateX(-50%)

[4] HEADER SOLAPAMIENTO resuelto
    · .card-title-wrap: padding-right 14px → 40px (despeja el botón × de 24px a right:10px)
    · .card-remove-btn: añadido z-index:5 (siempre por encima del contenido)

[5] ANILLOS — ratio proporcional a datos reales
    · Antes: main ring siempre 0.833 (totalIngr/totalIngr*1.2 = constante)
    · Ahora: arco refleja presencia y magnitud real de los datos
    · prevIngr pasado a _renderEvRings para comparación relativa

[6] ANÁLISIS FINANCIERO — wired correctamente
    · _renderEvAnalysis llamado después de rings y chart

node --check: 10/10 ✅  |  16.424 líneas
