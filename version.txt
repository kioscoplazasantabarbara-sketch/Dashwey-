Dashwey v9.5.69

[1] REORDENACIÓN: "Evolución de Ventas" ahora primera tarjeta
    · Posicionada visualmente ANTES del Beneficio Neto hero
    · Beneficio Neto permanece como resumen fijo debajo
    · Ambas siguen siendo draggables en su contenedor

[2] BUG CRÍTICO VENTAS — Pipeline de datos corregido
    · Causa raíz: renderVentas hacía v.pvp * v.qty a nivel raíz
      pero TPV guarda ventas con items:[{prodId, qty, pvp}]
      → resultado siempre 0 (undefined * undefined)
    · Fix: lee items[] con reduce, fallback a root para legado
    · Aplica en: totalIngr, totalUds, ticketMed
    · Aplica en: _renderEvChart (buckets horarios y diarios)

[3] DELTA % REALES
    · Antes: hardcoded (+5.2%, +12.4%, +2.1%)
    · Ahora: calcula período anterior real con _prevPeriod()
    · Compara ventas actuales vs período anterior

[4] SNAP NAVEGACIÓN — velocity-based
    · Antes: solo umbral 55px → podía quedar a medias
    · Ahora: umbral 55px OR velocidad > 0.3 px/ms
    · Siempre snappea al tab completo (nunca estado intermedio)
    · Captura timestamp en touchstart para cálculo de velocidad

[5] DashweyBus 'venta'
    · Añadido renderVentas() explícito tras registrar venta
    · Chart y anillos se actualizan en tiempo real tras cobro

node --check: 10/10 ✅  |  16.214 líneas
