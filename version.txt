{
  "version": "9.5.31",
  "build": "v9.5.31",
  "lastUpdate": "2026-03-21T20:00:00Z",
  "changelog": "FIX DEFINITIVO splash duplicado en Android/PWA. El splash es ahora HTML estatico puro — primer elemento del <body>. Los estilos estan en el <head> como CSS inline. El browser lo pinta en el primer frame sin ninguna dependencia de JS. JS solo oculta el splash via _hideSplash(). Elimina el race condition entre el primer paint del browser y la ejecucion de JS que existia en versiones anteriores."
}
