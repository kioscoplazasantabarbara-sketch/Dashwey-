Dashwey v9.5.80

[UNIFICACIÓN MENÚ ALM — REWORK SEGURO desde v9.5.79]

CAUSA DE REGRESIÓN EN PRIMER INTENTO v9.5.80:
  · alm._naSave se asignó como propiedad dinámica fuera del return del módulo
  · App.alm._naSave() no existía en el objeto expuesto → crash al guardar
  · Este rework elimina por completo alm._naSave — sin funciones dinámicas

[1] CSS — .sac-wrap unificado con .fi (Crimson)
    · background: #EDEDEE; border-radius: var(--r-md)
    · :focus-within → border-color:var(--red) + box-shadow crimson
    · .sac-input-row: padding 0 14px, height 52px
    · .sac-clear: 22px, transición suave

[2] openNuevoArtFs — 4 cambios quirúrgicos (sin reescribir la función)
    A) onfocus eliminado de campo Artículo (ya no abre SAC al tocar)
    B) onfocus eliminado de campo Proveedor
    C) onfocus eliminado de campo Categoría
    D) Dos botones → checkbox "Añadir al carrito" + botón único
       · Checkbox con animación visual inline (onchange inline seguro)
       · Botón "Guardar artículo": onclick inline directo sin función auxiliar
         document.getElementById('na-add-cart')?.checked
           ? App.alm.saveArtAndAddCart()
           : App.alm.saveArt()
       · saveArt() y saveArtAndAddCart() sin modificar — intactos

[3] openEditArt — 3 cambios visuales (sin tocar la lógica)
    A) Header: gradiente surface→red-s, subtítulo nombre en crimson
    B) #ea-rent: background var(--red-s), título "Análisis financiero" en rojo
    C) Botones Borrar/Guardar: height:52px, icono SVG en Borrar

[SW]       CACHE_NAME: dashwey-v9-5-79 → dashwey-v9-5-80
[manifest] v=9.5.79 → v=9.5.80
[HTML]     _APP_VERSION: v9.5.79 → v9.5.80 | 16.707 líneas

─────────────────────────────────────────
QA — PASOS CRÍTICOS
─────────────────────────────────────────
□ Nuevo artículo → tap en campo Nombre → SAC NO aparece automáticamente
□ Escribir en Nombre → SAC aparece con sugerencias
□ Checkbox desmarcado → Guardar → solo guarda (no añade carrito)
□ Checkbox marcado → Guardar → guarda Y añade al carrito
□ Editar artículo → header con nombre en rojo
□ Análisis financiero en editArt → fondo red-s, título rojo
□ Botones Borrar/Guardar en editArt → misma altura (52px)
□ Pull-down refresh → sin regresiones
□ Navegación entre tabs → sin regresiones
