Dashwey v1.2.26-dev
Build: v1.2.26-dev
Date: 2026-03-24

CHANGELOG v1.2.26-dev — Sheet System & GPU Optimization
=========================================================

[FIX-1] --sheet-duration unificado a 0.22s (era 0.26s)
  - Token único del sistema: todos los sheets, modales y overlays
  - Dentro del rango estándar 180–220ms
  - CLOSE_DUR JS alineado a 220ms

[FIX-2] will-change eliminado de #fs-box (settings sheet)
  - Era permanente → GPU layer ocioso constante
  - Activado en touchstart, liberado en snapBack/commitClose/touchcancel

[FIX-3] will-change eliminado de #modal-sheet
  - Mismo patrón que FIX-2, misma solución

[FIX-4] #swipe-track duration 0.30s → 0.22s
  - Unificado con el estándar del sistema

[FIX-5] Settings sheet: snap-to-half eliminado
  - Swipe rápido (vel > 0.4px/ms) o > 40% desplazamiento → cierre directo
  - _snapToHalf() y PCT_HALF eliminados (dead code)
  - Swipe-up desde half → expand a full se mantiene

VALIDACIÓN
- Braces JS: { 3526 } 3526 — balance perfecto
- Parens JS: diff -2 (igual al original, sin regresión)
- version.json, sw.js, title, manifest: sincronizados a v1.2.26-dev
