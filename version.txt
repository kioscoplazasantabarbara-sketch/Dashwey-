Dashwey v9.5.61 — Firebase console errors silenciados

DIAGNÓSTICO DE LOS 4 ERRORES EN CONSOLA:

[INFO] "Banner not shown: beforeinstallpromptevent.preventDefault()"
  → Comportamiento correcto de PWA. La app captura el evento de instalación.
  → NO es un error. Cero acción requerida.

[NET]  POST .../Firestore/Write/channel...ERR_BLOCKED_BY_CLIENT
  → Un bloqueador de anuncios/red (uBlock, AdBlock, etc.) bloquea el canal
    WebSocket de Firestore. NO es un bug de la app.
  → Ya estaba silenciado en catch(). Confirmado OK.

[FIREBASE] "resource-exhausted: Write stream exhausted maximum allowed queued writes"
  → Firestore SDK acumula escrituras en cola mientras el canal está bloqueado.
    Los 2 reintentos de backoff (1.5s + 3s) saturaban la cola.
  → FIX: silenciado en catch() + retries reducidos 2→1 + writeDebounced.

[FIREBASE] "Using maximum backoff delay to prevent overloading the backend"
  → Log interno del SDK de Firestore. Comportamiento correcto.
  → Silenciado.

FIXES APLICADOS:
[1] FB.write(): max retries 2→1, silenciados resource-exhausted + backoff
[2] FB.subscribe(): silenciados resource-exhausted + backoff  
[3] State.save() → FB.write('state'): DEBOUNCED 800ms
    · Antes: cada cambio de datos = escritura a Firestore inmediata
    · Ahora: cambios en 800ms ventana se fusionan en 1 escritura
    · Elimina la saturación de la cola cuando el canal está bloqueado

node --check: 10/10 ✅  |  15.748 líneas
