Dashwey v9.5.71

[1] BUG CRÍTICO — Barras invisibles: RESUELTO
    · Causa raíz confirmada: _renderEvChart hacía v.pvp*v.qty a nivel raíz
      Las ventas del TPV tienen estructura {items:[{pvp,qty}]} — raíz undefined
      Todos los buckets = 0 → scaleY(0) → todas las barras invisibles
    · Fix: lee items[] con reduce en ambos paths (horario y diario)
    · Fallback a root-level para ventas antiguas

[2] Métricas de anillos (Ventas Netas, Recibos, Prom.)
    · También afectadas por el mismo bug items[]
    · Confirmado que el fix v9.5.69 estaba presente — ventas correctas

[3] Análisis financiero debajo del gráfico
    · Bloque .ev-analysis-block con fondo crimson sutil
    · Insights derivados de datos reales:
      - Ticket medio (€) y número de ventas
      - Hora pico del día actual (si hay ventas hoy)
      - Tendencia vs período anterior (% delta con color)
    · Solo visible cuando hay datos (display:none cuando vacío)

[4] Drag & drop — feedback visual completo
    · .dash-drop-placeholder: borde dashed crimson (dónde caerá la card)
    · .is-dragging: box-shadow elevado + scale(1.03)
    · .activating: animación cardActivate 0.28s en long-press
    · .drag-mode .dash-draggable: dim cards no activas
    · Clone: posición corregida (_dragStartY se fija ANTES de cssText)

[5] Snap navegación — velocity restaurado
    · Threshold 55px OR velocidad > 0.3 px/ms
    · _touchStartTime capturado en touchstart
    · Siempre snappea a tab completo — nunca estados intermedios

node --check: 10/10 ✅  |  16.340 líneas
