Dashwey v9.5.81

[HEADER ALM — PROVEEDOR ACTIVO CLICKABLE]

[1] HTML — Header ALM unificado (alm-page-header + alm-prov-bar → 1 sola sección)
    Antes:
    · alm-page-header: "Almacén" + "Control de stock y pedidos" (fijo)
    · alm-prov-bar: elemento separado con icono + "Proveedor activo" + nombre + ▼
    · Dos secciones ocupando espacio doble en el scroll área
    Después:
    · Un único alm-page-header con:
      - Título: "Almacén" (sin cambios)
      - Subtítulo: "Proveedor activo: [Nombre] ▾" — todo en una línea
        · "Proveedor activo:" en color ink-mid (gris)
        · [Nombre] en negrita crimson, clickable → abre openProvPicker()
        · ▾ SVG chevron crimson como indicador de interacción
    · El catálogo y buscador suben automáticamente al ocupar menos espacio
    · alm-prov-bar → display:none (CSS)

[2] JS — updateProvBar() actualizado
    · Ahora actualiza #alm-prov-name dentro del botón del subtítulo
    · #alm-prov-icon se mantiene como span oculto (compatibilidad)
    · #alm-prov-change-btn se mantiene como button oculto (compatibilidad)
    · Todas las funciones que llaman updateProvBar() funcionan igual:
      selProv(), shiftProv(), saveProvEdit(), borrarProv(), etc.

[3] CSS
    · alm-page-header: padding-bottom 14px→12px (barra eliminada)
    · #alm-prov-sub-btn:active → opacity:0.65 + translateY(1px) microfeedback
    · alm-prov-bar → display:none (CSS cleanup)

[SW]       CACHE_NAME: dashwey-v9-5-80 → dashwey-v9-5-81
[manifest] v=9.5.80 → v=9.5.81
[HTML]     _APP_VERSION: v9.5.80 → v9.5.81 | 16.710 líneas

─────────────────────────────────────────
QA — PASOS DE VALIDACIÓN
─────────────────────────────────────────
□ Header ALM: "Proveedor activo: Coca-Cola CCEP ▾" visible como subtítulo
□ Tap en nombre proveedor → abre sheet selector de proveedor
□ Seleccionar proveedor diferente → subtítulo se actualiza con el nuevo nombre
□ shiftProv (← ›) en sub-nav → nombre en subtítulo se actualiza
□ Buscador y catálogo: visible directamente sin scrollear (espacio liberado)
□ Pull-to-refresh → nombre de proveedor correcto tras refresh
□ Microfeedback: tap en nombre → ligero desplazamiento visual y opacity
□ Navegación entre tabs → sin regresiones
□ Dashboard KPIs y TPV → sincronización intacta
