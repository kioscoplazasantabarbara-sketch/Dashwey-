Dashwey v9.5.70

[1] BUG: Beneficio Neto desbloqueada y draggable
    · Era una bn-card FUERA del dash-cards-container (nunca participó en drag)
    · Ahora: class="card dash-draggable" data-card-id="beneficio-neto" DENTRO del container
    · Eliminado el bloque duplicado fuera del container
    · Registrada en _CARD_IDS, _CARD_LABELS, validIds, cardHasActivity, pinnedCards

[2] DOM duplicado eliminado
    · Existían 2 instancias de data-card-id="ventas" con el mismo id — DOM inválido
    · Eliminado el duplicado externo; solo queda la instancia dentro del container

[3] bn-card CSS: margin/hero positioning eliminado
    · .bn-card ya no tiene margin:10px 12px (era para uso hero standalone)
    · El grid del container maneja el espaciado normalmente

[4] Evolución de Ventas — chart taller
    · ev-chart-bars y ev-chart-y: height 120px → 150px
    · Barras más visibles, datos más legibles

[5] Top Artículos — compacto + modal expandido
    · ta-item: padding 12px→8px, thumb 56px→44px, fuentes reducidas
    · Click/tap en header → openTopVentasModal(): bottom sheet completo
      con todos los artículos vendidos en el período seleccionado

[6] Glassmorphism controlado
    · #header:        rgba(247,242,242,0.88) + blur(12px) + crimson border
    · #bottom-tabs:   rgba(247,242,242,0.88) + blur(12px) + crimson border-top
    · #fs-box:        rgba(247,242,242,0.96) + blur(20px)
    · #modal-sheet:   rgba(247,242,242,0.96) + blur(20px)
    · Dark mode: rgba(12,12,14,0.92) para todos
    · Tarjetas del Dashboard: sin glassmorphism (solo fondo sólido)

node --check: 10/10 ✅  |  16.263 líneas
