Dashwey v9.5.62 — Flicker definitivamente eliminado + Beneficio Neto

CAUSA RAÍZ DEL PARPADEO IDENTIFICADA Y CORREGIDA:

[1] renderPedidosCalendario(): style.width = pct + '%' → scaleX()
    · Era el ÚNICO lugar que aún usaba style.width en el rAF
    · style.width = layout property → fuerza reflow en TODA la página
    · scaleX() = compositor-only → cero layout recalc
    · También: style="width:0" inline eliminado del HTML generado

[2] renderAgenda(): style="width:0" + style="left:0" eliminados
    · Estos inline styles sobrescribían el CSS scaleX/--icon-pct
    · Con ellos presentes, las transiciones CSS no funcionaban
    · Ahora: CSS controla todo, JS solo escribe data-pct en el DOM

[3] Stagger loop en render() eliminado
    · querySelectorAll('.dash-card') = clase inexistente → 0 resultados
    · rAF vacío ejecutándose en cada render → overhead sin beneficio

[4] Toast "Sincronizado con otro dispositivo" ELIMINADO
    · Completamente suprimido — sync es silencioso

[5] Beneficio Neto — rediseño left-aligned
    · Título "Beneficio Neto" alineado a la izquierda como todas las tarjetas
    · Separador ultra-delgado edge-to-edge con gradient difuminado (.bn-separator)
    · Valor principal en bloque propio (.bn-value-wrap) debajo del separador
    · Ventas, Inversión, ROI en stats row jerárquico debajo
    · Borde crimson sutil (rgba(139,26,47,0.10)) para microdiferenciación
    · ROI color variable corregido (var(--green) en lugar de hardcode)

node --check: 10/10 ✅  |  15.770 líneas
