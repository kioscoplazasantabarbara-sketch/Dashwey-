Dashwey v9.5.60 — Flicker definitivamente eliminado

CAUSAS RESIDUALES IDENTIFICADAS Y CORREGIDAS:

[1] DUPLICACIÓN de iconos en Agenda eliminada
    · agenda-type-icon (columna izquierda) estaba duplicando el icono del riel
    · Ahora solo existe el icono dentro de .tl-track-icon en la barra animada

[2] Inline style="width:0" y style="left:0" eliminados del HTML de track
    · Estos overrides CSS rompían el scaleX/--icon-pct approach
    · CSS controla todo, JS solo escribe valores numéricos

[3] Drag & drop — getBoundingClientRect eliminado del touchmove
    · findInsertPosition llamaba getBCR() en CADA tarjeta en CADA touchmove
    · = N layout reads por frame durante el drag = reflow cascade = flicker
    · Ahora: todos los midpoints se cachean en startDrag (un batch read único)
    · moveDrag usa el cache — CERO lecturas de layout durante el drag

[4] Drag clone usa transform:translateY en lugar de style.top
    · style.top = geometry change = layout recalc cada frame
    · transform:translateY = compositor-only = cero layout

[5] moveDrag throttle: placeholder solo se reordena si movimiento > 6px
    · Elimina insertBefore() calls innecesarios en micromovimientos

[6] agenda-card isolation:isolate
    · Crea stacking context propio: shimmer ::before no puede salir del scope

[7] "Análisis financiero" en lugar de "Recomendación IA" + color crimson

[8] Sync toast sin emoji + drag end toast sin emoji

node --check: 10/10 ✅  |  15.728 líneas
