Dashwey v9.5.75

[AUDITORÍA FORENSE — 7 FIXES DE PRODUCCIÓN]

[1] BUG — _APP_VERSION desactualizado (L6463)
    · _APP_VERSION = 'v9.5.73' en archivo v9.5.74
    · checkForUpdate() comparaba contra versión incorrecta
    · Fix: actualizado a 'v9.5.74' (versión real del binario)

[2] BUG — showWhatsNew hardcoded a versión anterior (L15283)
    · init() llamaba showWhatsNew('v9.5.73') explícito
    · Usuario veía changelog de versión anterior al abrir la app
    · Fix: reemplazado por App.ui._getAppVersion() — nunca más desincronizado

[3] PERF — _shimmerAllCards generaba N setTimeout por card (L8968)
    · _CARD_IDS = 8 elementos → 8 setTimeout dentro del rAF forEach
    · Fix: un único setTimeout global al delay máximo total
      De 8 macrotask timers a 1 solo cleanup

[4] PERF — _animateCardAppear usaba setTimeout(340) magic number
    · Fix: addEventListener('animationend', ..., {once:true})
      Cleanup exacto al final real de la animación CSS

[5] DEAD_CODE — renderNS() vacía exportada en API pública del módulo alm
    · function renderNS() {} sin cuerpo — legacy eliminado en versiones anteriores
    · Fix: eliminada del return del módulo (stub interno preservado)

[6] DUPLICATE — Listener vacío dashwey:venta-registrada (L15708)
    · window.addEventListener sin ninguna acción dentro
    · Manejado completamente por DashweyBus.on('venta')
    · Fix: eliminado — DashweyBus es SSOT

[7] STATE — _openIngresoSheet setTimeout anidado aplanado (L9555)
    · setTimeout(ANIM_DELAY) contenía setTimeout(50) para esperar DOM
    · Fix: requestAnimationFrame() reemplaza setTimeout(50)
      rAF garantiza DOM pintado antes de adjuntar listeners. Estructura plana.

[SW]       CACHE_NAME: dashwey-v9-5-74 → dashwey-v9-5-75
[SW]       postMessage version: '9.5.74' → '9.5.75'
[manifest] start_url + iconos: v=9.5.74 → v=9.5.75
[HTML]     7 fixes quirúrgicos — 16.434 líneas

Líneas: 16.434  |  Fixes: 7/7 ✅
