Dashwey v1.2.32-dev
Build: v1.2.32-dev
Date: 2026-03-25

CHANGELOG v1.2.32-dev — Selector global de periodo: bugs corregidos y sincronización completa
==============================================================================================

[BUG-C1 CRÍTICO] Presets nunca se marcaban como seleccionados (checkmark visual ausente)
  - _onPeriodSheetSel: usaba el.getAttribute('onclick') para marcar .sel
  - Los opts del sheet usan data-t (no onclick attr) desde v9.5.104b
  - FIX: el.classList.toggle('sel', el.dataset.t === t) en ambas funciones
  - Afectaba: _onPeriodSheetSel (Utils) y selGlobalPeriod (dash)

[BUG-C2 CRÍTICO] Custom 'Personalizado' tampoco se marcaba como .sel
  - Mismo bug en selGlobalPeriod rama t==='custom'
  - FIX: misma corrección dataset.t

[FEAT-1] KPI.getPresetRange(type) — función exportada con normalización de horas
  - type: 'today'|'yesterday'|'week'|'month'|'last30'|'year'|'hours'
  - Normaliza: 00:00:00.000 inicio del día, 23:59:59.999 fin del día
  - 'last30': últimos 30 días desde hoy (00:00 → 23:59 hoy)
  - Devuelve { start: Date, end: Date }
  - Diferencia de periodRange(): no necesita un period object, solo el tipo

[FEAT-2] Evento global document.dispatchEvent(new Event('date:changed'))
  - Emitido en: selGlobalPeriod, applyGlobalCustom, shiftPeriod, onCustom callback
  - Cubre todos los caminos de cambio de periodo en la app
  - Listener registrado una sola vez (_dateChangedBound guard)

[FEAT-3] appState.dateRange sincronizado en todos los cambios de periodo
  - { from: Date, to: Date } actualizado en selGlobalPeriod, applyGlobalCustom,
    shiftPeriod y openGlobalPeriodPicker.onCustom
  - Inicializado al arranque desde el periodo persistido en State (localStorage)

[FEAT-4] Guard de "rango sin cambios" — evita doble render
  - selGlobalPeriod: si current.type === t && !current.offset → no re-render
  - applyGlobalCustom: si customStart y customEnd idénticos → no re-render

[FEAT-5] shiftPeriod emite date:changed y actualiza appState.dateRange
  - Consistencia: cualquier cambio de periodo (incluyendo flechas izq/der)
    dispara el evento y actualiza el mirror

VALIDACIÓN
  - ✅ Presets se marcan visualmente al seleccionarlos (BUG-C1 corregido)
  - ✅ 'Personalizado' se marca al abrir el form custom (BUG-C2 corregido)
  - ✅ date:changed emitido en 4 puntos + listener registrado sin duplicados
  - ✅ appState.dateRange inicializado al arranque y actualizado en cada cambio
  - ✅ Guards evitan render doble si el periodo es idéntico
  - ✅ KPI.getPresetRange() disponible globalmente via App.KPI.getPresetRange()
  - ✅ CACHE_NAME sw.js == CURRENT_CACHE index.html: dashwey-v1-2-32-dev
  - ✅ 0 cambios en comportamiento existente — solo correcciones y adiciones
