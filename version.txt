Dashwey v1.0.2

[Notificaciones nativas de actualización]

═══════════════════════════════════════
CAMBIOS v1.0.2
═══════════════════════════════════════

1. NOTIFICACIÓN NATIVA ANDROID AL DETECTAR UPDATE
   - _silentCheckUpdate() ahora llama _notifUpdate() al detectar nueva versión
   - La notificación se envía via SW → aparece en Android aunque la app esté minimizada
   - Guard: solo si Notification.permission === 'granted' + notifOn + prefs.whatsNew
   - Guard de foreground: si la app está visible, el banner ya es suficiente (sin duplicar)

2. NOTIFICACIÓN DESDE SW (APP CERRADA)
   - En activate: si no hay clientes activos, el SW muestra showNotification directamente
   - Cubre el caso: Dashwey cerrada + nuevo SW se activa en background → notificación

3. DEEP LINK AL TOCAR LA NOTIFICACIÓN
   - action: 'update' → abre app + muestra banner de actualización listo para aplicar
   - DEEP_LINK handler actualizado en index.html

4. WHAT'S NEW v1.0.2
   - Entrada en changelog: "Notificaciones de actualización"

═══════════════════════════════════════
ARCHIVOS ENTREGADOS
═══════════════════════════════════════

  index.html    ← _notifUpdate() + deep link 'update' + What's New v1.0.2
  sw.js         ← CACHE_NAME: dashwey-v1-0-2 + notif en activate
  version.json  ← version: 1.0.2
  version.txt   ← este archivo

═══════════════════════════════════════
QA
═══════════════════════════════════════
□ Subir updatev106 a GitHub
□ Minimizar app en Android
□ Esperar ~8s (auto-check al arrancar) o 5min (check periódico)
□ Recibir notificación "🆕 Dashwey v1.0.2 disponible"
□ Tocar notificación → app abre con banner de actualización visible
□ What's New v1.0.2 aparece al primer arranque
